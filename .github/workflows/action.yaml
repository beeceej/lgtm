on:
  issue_comment:
    types: [created]

jobs:
  lgtm:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: LGTM
      uses: ./
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
